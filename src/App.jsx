import React, { useMemo, useState } from "react";
import { motion } from "framer-motion";
import {
  AlignCenter, AlignLeft, Bold, CheckCircle2, ChevronDown, FileText,
  GraduationCap, HelpCircle, Highlighter, MousePointerClick, PartyPopper,
  PlayCircle, RotateCcw, Sparkles, Type,
} from "lucide-react";

function Card({ className = "", children }) { return <div className={className}>{children}</div>; }
function CardContent({ className = "", children }) { return <div className={className}>{children}</div>; }
function Button({ className = "", variant = "default", disabled = false, children, ...props }) {
  const base = "inline-flex items-center justify-center rounded-md px-4 py-2 text-sm font-semibold transition focus:outline-none focus:ring-2 focus:ring-emerald-400 disabled:pointer-events-none disabled:opacity-50";
  const variantClass = variant === "outline" ? "border border-slate-300 bg-white text-slate-900 hover:bg-slate-50" : "bg-emerald-700 text-white hover:bg-emerald-800";
  return <button className={`${base} ${variantClass} ${className}`} disabled={disabled} {...props}>{children}</button>;
}
function Progress({ value = 0, className = "" }) {
  return <div className={`h-3 w-full overflow-hidden rounded-full bg-slate-200 ${className}`}><div className="h-full rounded-full bg-emerald-600 transition-all" style={{ width: `${value}%` }} /></div>;
}

const correctDate = "June 15, 2026";
const correctTitle = "The Effects of Social Media on Student Success";
const templateUrl = "https://docs.google.com/document/d/1zMVGaUEyLj6AtYG3hSZwfPFuOqJdcZeh/template/preview";

const initialState = {
  margins: "Default", font: "Arial", fontSize: "11", spacing: "Single",
  pageNumberAdded: false, runningHeadRemoved: false,
  title: "the effects of social media on student success",
  titlePlaced: false, titlePageCentered: false, titleBold: false,
  blankLineAfterTitle: false, selectedDate: "15 June 2026",
  titlePageOrderCorrect: false, paragraphIndented: false,
  levelOneHeadingFormatted: false,
};

const choices = {
  placement: ["Start the title at the very top of the page.", "Begin the title about 3–4 lines down from the top of the page.", "Move the title to the bottom of the page."],
  centering: ["Center the entire title page except for the page number.", "Left align everything on the title page.", "Center only the student name and due date."],
  bold: ["Bold the paper title only.", "Bold the student name and college/university.", "Bold every line on the title page."],
  blank: ["Add one extra double-spaced blank line between the title and student name.", "Add three blank lines between every title page line.", "Do not leave any space between the title and student name."],
  title: ["the effects of social media on student success", "The effects of social media on student success", "The Effects of Social Media on Student Success"],
  order: ["Title, Student Name, Full College/University Name, Course Name and Number, Instructor Name, Assignment Due Date", "Student Name, Title, Instructor Name, Course Name and Number, Full College/University Name, Assignment Due Date", "Title, Instructor Name, Student Name, Assignment Due Date, Full College/University Name, Course Name and Number"],
  date: ["15 June 2026", "June 15, 2026", "6/15/2026"],
  heading: ["Centered and bold.", "Left aligned and italicized.", "Underlined and all caps."],
};

const steps = [
  { id: "margins", short: "Margins", color: "from-sky-400 to-blue-500", goal: "Check the page margins.", doThis: ["Click File.", "Choose Page setup.", "Check that each margin is set to 1 inch.", "Click Apply."], why: "APA student papers use 1-inch margins on all sides. Google Docs usually uses this setting already, but students should know how to check it.", success: "Great job! You checked the page margins.", hint: "Look at the top menu. File is on the far left.", showMe: "File → Page setup → Margins: 1 inch → Apply", check: s => s.margins === "1 inch", solve: { margins: "1 inch" } },
  { id: "font", short: "Font", color: "from-emerald-400 to-green-500", goal: "Use an acceptable APA font.", doThis: ["Click the font menu that says Arial.", "Choose Times New Roman."], why: "APA allows several readable fonts. In this lab, you will practice with Times New Roman, which is commonly accepted in college papers.", success: "Great job! You changed the paper to Times New Roman.", hint: "The font menu is in the toolbar near the top.", showMe: "Font menu → Times New Roman", check: s => s.font === "Times New Roman", solve: { font: "Times New Roman" } },
  { id: "size", short: "Size", color: "from-violet-400 to-purple-500", goal: "Make the font 12-point size.", doThis: ["Click the size box that says 11.", "Choose 12."], why: "When using Times New Roman in APA, 12-point font is commonly used and keeps the paper readable.", success: "Great job! The paper is now set to 12-point font.", hint: "The font size box is beside the font menu.", showMe: "Font size menu → 12", check: s => s.fontSize === "12", solve: { fontSize: "12" } },
  { id: "spacing", short: "Spacing", color: "from-amber-400 to-orange-500", goal: "Set the paper to double spacing.", doThis: ["Click the Line Spacing button beside the alignment buttons.", "Select Double."], why: "APA papers are double spaced throughout, including the title page and body paragraphs.", success: "Great job! You changed the paper to double spacing.", hint: "The Line Spacing button has lines with up and down arrows.", showMe: "Line Spacing button → Double", check: s => s.spacing === "Double", solve: { spacing: "Double" } },
  { id: "titlePlacement", short: "Title Placement", color: "from-blue-400 to-indigo-500", goal: "Move the APA title page information down.", doThis: ["Look at the APA paper on the left.", "Answer the question below by selecting where the title should begin."], why: "On an APA student title page, the title should begin about 3–4 lines down from the top of the page.", success: "Great job! The title page information now begins in the correct area.", hint: "The title should not begin at the very top of the page.", showMe: "Begin the title about 3–4 lines down from the top of the page.", check: s => s.titlePlaced, solve: { titlePlaced: true }, custom: "placement" },
  { id: "titlePageCentering", short: "Center Page", color: "from-cyan-400 to-teal-500", goal: "Center the APA title page information.", doThis: ["Look at the APA paper on the left.", "Select the answer below that explains how the alignment should be fixed."], why: "The title, author name, school, course, instructor, and due date should be centered on the APA student title page. The page number remains right aligned.", success: "Great job! The title page information is centered, and the page number stays on the right.", hint: "Everything on the title page is centered except the page number.", showMe: "Center the title page information. Keep the page number in the upper-right corner.", check: s => s.titlePageCentered, solve: { titlePageCentered: true }, custom: "centering" },
  { id: "pageNumber", short: "Page Number", color: "from-sky-500 to-cyan-500", goal: "Add the APA page number.", doThis: ["Click Insert.", "Choose Page number → Top right.", "Make sure only the page number appears in the header."], why: "APA student papers use a page number in the top-right header. Student papers do not need a running head unless your instructor specifically asks for one.", success: "Great job! You added the page number only.", hint: "Do not type the title in the header. APA student papers usually need only the page number.", showMe: "Header should show only: 1", check: s => s.pageNumberAdded && s.runningHeadRemoved, solve: { pageNumberAdded: true, runningHeadRemoved: true }, custom: "pageNumber" },
  { id: "boldTitle", short: "Bold Title", color: "from-fuchsia-400 to-pink-500", goal: "Bold only the paper title.", doThis: ["Look at the APA paper on the left.", "Answer the question below: Which element should be bolded?"], why: "In APA format, the paper title is bolded. The student name, college/university, course, instructor, and due date should remain regular font.", success: "Excellent! Only the paper title is bolded.", hint: "Do not bold the student name or course information.", showMe: "Bold the paper title only.", check: s => s.titleBold, solve: { titleBold: true }, custom: "bold" },
  { id: "blankLine", short: "Blank Line", color: "from-rose-400 to-pink-500", goal: "Add the correct spacing after the title.", doThis: ["Look at the space between the title and student name.", "Select the answer below that fixes the spacing."], why: "APA title pages include one extra double-spaced blank line between the paper title and the author's name.", success: "Great job! You added the extra double-spaced blank line after the title.", hint: "There should be a little more space after the title than between the student information lines.", showMe: "Title → extra double-spaced blank line → Author name", check: s => s.blankLineAfterTitle, solve: { blankLineAfterTitle: true }, custom: "blank" },
  { id: "titleCase", short: "Title Case", color: "from-purple-400 to-fuchsia-500", goal: "Fix the APA title capitalization.", doThis: ["Look at the APA paper on the left.", "Answer the question below by selecting the title that fixes it correctly."], why: "APA titles use title case on the title page, which means the important words are capitalized.", success: "Great job! You fixed the APA title capitalization.", hint: "Capitalize the first word, last word, and important words.", showMe: "Correct title: The Effects of Social Media on Student Success", check: s => s.title === correctTitle, solve: { title: correctTitle }, custom: "title" },
  { id: "studentInfo", short: "Student Info", color: "from-orange-400 to-red-500", goal: "Place the APA student title page information in the correct order.", doThis: ["Look at the student information in the APA paper on the left.", "Select the correct order below."], why: "An APA student title page includes the title, student name, college/university, course, instructor, and due date in a specific order.", success: "Great job! You selected the correct APA student title page order.", hint: "The title appears first, followed by student name, college/university, course, instructor, and due date.", showMe: "Title → Student Name → Full College/University Name → Course → Instructor → Due Date", check: s => s.titlePageOrderCorrect, solve: { titlePageOrderCorrect: true }, custom: "order" },
  { id: "dateFormat", short: "Due Date", color: "from-amber-500 to-orange-500", goal: "Fix the due date format.", doThis: ["Look at the due date in the APA paper on the left.", "Select the date format used for APA student papers in this lab."], why: "APA student papers commonly use the month-day-year format for the assignment due date, such as June 15, 2026.", success: "Great job! You selected the correct due date format.", hint: "Use Month Day, Year.", showMe: "Correct format: June 15, 2026", check: s => s.selectedDate === correctDate, solve: { selectedDate: correctDate }, custom: "date" },
  { id: "indent", short: "Page 2 Indent", color: "from-lime-400 to-green-500", goal: "Move to Page 2 and tab the first paragraph inward.", doThis: ["You are now viewing Page 2 of the APA paper.", "Locate the Press Tab button is in the APA Coach panel below.", "Click the Press Tab button and watch the first line move inward."], why: "The title page and the body of the paper are different pages. On Page 2, the title appears again at the top, and each body paragraph begins with a first-line indent.", success: "Great job! The first paragraph on Page 2 is now correctly indented.", hint: "The Press Tab button below is in the APA Coach panel directly under these instructions.", showMe: "On Page 2, click Press Tab in the APA Coach panel.", check: s => s.paragraphIndented, solve: { paragraphIndented: true }, custom: "tab" },
  { id: "heading", short: "Optional Heading", color: "from-emerald-400 to-teal-500", goal: "Format an optional APA Level 1 subheading.", doThis: ["Look at the optional heading on Page 2.", "Select how a Level 1 APA heading should be formatted."], why: "APA papers sometimes use subheadings to organize longer papers. Subheadings are optional unless your instructor requires them. Not every assignment requires headings, but if you use a Level 1 heading, it should be centered and bold.", success: "Great job! You formatted the optional APA Level 1 subheading correctly.", hint: "A Level 1 heading is centered and bold.", showMe: "Level 1 APA heading → Centered and bold", check: s => s.levelOneHeadingFormatted, solve: { levelOneHeadingFormatted: true }, custom: "heading" },
];

const paragraphText = "Students use digital tools every day to communicate, learn, and complete assignments. Social media can help students connect with others, but it can also become a distraction when used without purpose. Understanding how technology affects learning requires careful reading, research, and organization. APA formatting helps writers present research clearly and professionally. When students learn the structure of APA papers, they are better prepared to write in academic and workplace settings.";

function LineSpacingIcon() { return <span className="inline-flex items-center gap-1" aria-hidden="true"><span className="flex flex-col gap-[3px]"><span className="block h-[2px] w-5 rounded bg-current"/><span className="block h-[2px] w-5 rounded bg-current"/><span className="block h-[2px] w-5 rounded bg-current"/></span><span className="text-xs leading-none">↕</span></span>; }

function ChoiceBox({ title, subtitle, choices, selected, onSelect }) {
  return <div className="mt-4 rounded-2xl bg-white p-4 ring-1 ring-slate-200"><p className="text-sm font-bold uppercase tracking-wide text-slate-500">{title}</p>{subtitle && <p className="mt-1 text-sm text-slate-600">{subtitle}</p>}<div className="mt-3 grid gap-2">{choices.map(choice => <button key={choice} onClick={() => onSelect(choice)} className={`rounded-xl border px-4 py-3 text-left font-medium transition hover:bg-emerald-50 ${selected === choice ? "border-emerald-500 bg-emerald-50 text-emerald-800" : "bg-white"}`}>{choice}</button>)}</div></div>;
}

export default function APAFormattingLab() {
  const [state, setState] = useState(initialState);
  const [stepIndex, setStepIndex] = useState(0);
  const [openMenu, setOpenMenu] = useState(null);
  const [wrongAction, setWrongAction] = useState("");
  const [started, setStarted] = useState(false);
  const [selectedArea, setSelectedArea] = useState("document");
  const [helpLevel, setHelpLevel] = useState(0);
  const [showExample, setShowExample] = useState(false);
  const [studentName, setStudentName] = useState("");
  const [showPageSetup, setShowPageSetup] = useState(false);
  const [feedback, setFeedback] = useState("");
  const [certificateDownloaded, setCertificateDownloaded] = useState(false);
  const [templateOpened, setTemplateOpened] = useState(false);
  const [showCertificate, setShowCertificate] = useState(false);

  const current = steps[stepIndex];
  const currentComplete = current.check(state);
  const completedCount = steps.filter(step => step.check(state)).length;
  const progress = Math.round((completedCount / steps.length) * 100);
  const allComplete = completedCount === steps.length;
  const isLastStepComplete = stepIndex === steps.length - 1 && currentComplete;
  const showPageTwo = stepIndex >= steps.findIndex(step => step.id === "indent");
  const pageStyle = useMemo(() => ({ fontFamily: state.font === "Times New Roman" ? "'Times New Roman', Times, serif" : "Arial, sans-serif", fontSize: state.fontSize === "12" ? "12pt" : "11pt", lineHeight: state.spacing === "Double" ? "2" : "1.25" }), [state.font, state.fontSize, state.spacing]);

  const showWrongAction = (message = "Not quite. Follow the current step in the APA Coach before moving on.") => { setWrongAction(message); setFeedback(message); window.setTimeout(() => setWrongAction(""), 2500); };
  const completeStepAction = (stepId, updates) => { if (current.id !== stepId) { showWrongAction(); setOpenMenu(null); return; } setState(prev => ({ ...prev, ...updates })); setOpenMenu(null); setFeedback(""); };
  const chooseFromList = (choice, correctChoice, updates, correctMessage, wrongMessage) => { const ok = choice === correctChoice; if (ok) setState(prev => ({ ...prev, ...updates })); setFeedback(ok ? correctMessage : wrongMessage); };
  const chooseTitle = title => { setState(prev => ({ ...prev, title })); setFeedback(title === correctTitle ? "Correct! Important words in an APA title should be capitalized." : "Not quite. Capitalize the important words in the title. Try again."); };
  const chooseDate = date => { setState(prev => ({ ...prev, selectedDate: date })); setFeedback(date === correctDate ? "Correct! APA student paper due dates are written as Month Day, Year in this lab." : "Not quite. Use Month Day, Year. Try again."); };
  const chooseOrder = choice => { const ok = choice === choices.order[0]; if (ok) setState(prev => ({ ...prev, titlePageOrderCorrect: true })); setFeedback(ok ? "Correct! That is the proper order for an APA student title page." : "Not quite. Try the order that begins with the title, then student name, then school information."); };
  const pressTab = () => { if (current.id !== "indent") { showWrongAction(); return; } setState(prev => ({ ...prev, paragraphIndented: true })); setSelectedArea("indent"); setFeedback("Correct! You tabbed the first line inward on Page 2."); };
  const nextStep = () => { if (!currentComplete) return; setHelpLevel(0); setFeedback(""); setStepIndex(i => Math.min(i + 1, steps.length - 1)); };
  const activeClass = area => (selectedArea === area || current.id === area ? "ring-4 ring-sky-300 bg-sky-50/60 rounded-md" : "");
  const certificateId = useMemo(() => { const date = new Date(); const y = date.getFullYear(); const m = String(date.getMonth() + 1).padStart(2, "0"); const d = String(date.getDate()).padStart(2, "0"); const serial = String(Math.floor(date.getTime() % 100000)).padStart(5, "0"); return `APA-${y}${m}${d}-${serial}`; }, []);
  const downloadCertificate = () => { setCertificateDownloaded(true); window.setTimeout(() => { const el = document.getElementById("printable-certificate"); if (el) el.scrollIntoView({ behavior: "smooth", block: "center" }); }, 100); };
  const printCertificate = () => window.print();
  const openTemplate = () => setTemplateOpened(true);

  if (!started) return <StartScreen studentName={studentName} setStudentName={setStudentName} setStarted={setStarted} />;

  if (showCertificate && allComplete) return <CertificateScreen studentName={studentName} certificateId={certificateId} certificateDownloaded={certificateDownloaded} templateOpened={templateOpened} downloadCertificate={downloadCertificate} printCertificate={printCertificate} openTemplate={openTemplate} />;

  return <div className="min-h-screen bg-[#f1f3f4] text-slate-900">
    {showPageSetup && <PageSetup onCancel={() => setShowPageSetup(false)} onApply={() => { completeStepAction("margins", { margins: "1 inch" }); setShowPageSetup(false); }} />}
    <TopBar steps={steps} state={state} stepIndex={stepIndex} setStepIndex={setStepIndex} progress={progress} current={current} helpLevel={helpLevel} openMenu={openMenu} setOpenMenu={setOpenMenu} setShowPageSetup={setShowPageSetup} completeStepAction={completeStepAction} showWrongAction={showWrongAction} />
    <div className="grid gap-6 p-5 lg:grid-cols-[1fr_380px]"><main className="flex justify-center overflow-auto rounded-2xl bg-[#e8eaed] p-6 shadow-inner"><Paper state={state} current={current} showPageTwo={showPageTwo} pageStyle={pageStyle} activeClass={activeClass} setSelectedArea={setSelectedArea} /></main><aside className="space-y-4"><Coach current={current} state={state} feedback={feedback} wrongAction={wrongAction} currentComplete={currentComplete} stepIndex={stepIndex} steps={steps} helpLevel={helpLevel} setHelpLevel={setHelpLevel} setFeedback={setFeedback} setStepIndex={setStepIndex} completeStepAction={completeStepAction} choices={choices} chooseFromList={chooseFromList} chooseTitle={chooseTitle} chooseDate={chooseDate} chooseOrder={chooseOrder} pressTab={pressTab} isLastStepComplete={isLastStepComplete} nextStep={nextStep} setShowCertificate={setShowCertificate} showPageTwo={showPageTwo} /><ProgressCard steps={steps} state={state} stepIndex={stepIndex} setStepIndex={setStepIndex} completedCount={completedCount} progress={progress} showExample={showExample} setShowExample={setShowExample} /></aside></div>
  </div>;
}

function StartScreen({ studentName, setStudentName, setStarted }) {
  return <div className="min-h-screen bg-gradient-to-br from-emerald-600 via-sky-500 to-amber-400 p-5 text-slate-950"><div className="mx-auto flex min-h-screen max-w-5xl items-center justify-center"><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="grid overflow-hidden rounded-[2rem] bg-white shadow-2xl md:grid-cols-[1fr_.85fr]"><div className="p-8 md:p-10"><div className="mb-5 flex items-center gap-3"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-emerald-100 text-emerald-700"><GraduationCap className="h-8 w-8" /></div><div><p className="text-sm font-bold uppercase tracking-wide text-emerald-700">Interactive practice</p><h1 className="text-3xl font-black md:text-5xl">APA Formatting Lab</h1></div></div><p className="text-lg leading-relaxed text-slate-700">This lab will help you practice APA student paper formatting by correcting mistakes on a sample title page and then viewing Page 2 of the paper.</p><div className="mt-6 rounded-3xl bg-gradient-to-r from-emerald-100 via-sky-50 to-yellow-100 p-5 shadow-sm ring-2 ring-emerald-300"><p className="text-xl font-black text-emerald-900">🏆 EARN PARTICIPATION CREDIT!</p><div className="mt-3 space-y-2 text-sm font-semibold text-slate-800"><p>The APA formatting skills you learn in this activity can help you prepare polished academic and workplace documents.</p><p>📘 Learn APA student paper formatting.</p><p>🧾 Practice title page and Page 2 setup.</p><p>🚀 Build formatting habits that help future writing assignments.</p></div></div><div className="mt-6 rounded-3xl bg-yellow-50 p-5 ring-1 ring-yellow-200"><p className="font-bold text-yellow-900">How it works</p><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm text-yellow-950"><li>Read one task at a time in the APA Coach.</li><li>Use the toolbar, menus, pop-up boxes, and practice choices to fix the document.</li><li>Look for the green check mark when the step is correct.</li><li>Use the <strong>I’m Stuck</strong> button anytime you need help.</li></ol></div><div className="mt-5 rounded-3xl bg-slate-50 p-5"><p className="font-bold">You do not need to open Google Docs.</p><p className="mt-1 text-sm text-slate-600">This activity includes its own Google Docs-style practice workspace. Complete everything inside this lab.</p></div><div className="mt-6 rounded-3xl bg-white p-4 ring-1 ring-slate-200"><label className="block text-sm font-bold text-slate-700">Enter your full name for your completion certificate:</label><input value={studentName} onChange={e => setStudentName(e.target.value)} placeholder="First and Last Name" className="mt-2 w-full rounded-2xl border px-4 py-3 text-base focus:outline-none focus:ring-2 focus:ring-emerald-400" /></div><Button onClick={() => setStarted(true)} disabled={!studentName.trim()} className="mt-7 w-full rounded-2xl bg-emerald-700 py-6 text-lg hover:bg-emerald-800 disabled:opacity-50"><PlayCircle className="mr-2 h-5 w-5" /> Start Lab</Button>{!studentName.trim() && <p className="mt-2 text-center text-sm text-slate-500">Enter your full name to begin.</p>}</div><div className="bg-slate-950 p-8 text-white md:p-10"><div className="mb-5 flex items-center gap-2 text-yellow-300"><Sparkles className="h-5 w-5" /><p className="font-bold">Skills you will practice</p></div><div className="grid gap-3">{steps.map((step, i) => <div key={step.id} className={`rounded-2xl bg-gradient-to-r ${step.color} p-[2px]`}><div className="rounded-2xl bg-slate-950/85 px-4 py-3 text-sm font-semibold">{i + 1}. {step.short}</div></div>)}</div><p className="mt-6 text-sm text-slate-300">This lab focuses on APA page formatting. In-text citations and reference entries are not included.</p></div></motion.div></div></div>;
}

function CertificateScreen({ studentName, certificateId, certificateDownloaded, templateOpened, downloadCertificate, printCertificate, openTemplate }) {
  return <div className="min-h-screen bg-gradient-to-br from-emerald-500 via-sky-500 to-fuchsia-500 p-6"><div className="mx-auto flex min-h-screen max-w-4xl items-center justify-center"><motion.div initial={{ opacity: 0, scale: 0.96 }} animate={{ opacity: 1, scale: 1 }} className="w-full rounded-[2rem] bg-white p-8 text-center shadow-2xl md:p-12"><PartyPopper className="mx-auto h-16 w-16 text-amber-500" /><h1 className="mt-4 text-4xl font-black text-slate-950">APA Formatting Mastered!</h1><p className="mx-auto mt-3 max-w-2xl text-slate-600">{studentName}, you successfully repaired the document and practiced APA student paper formatting skills.</p><div className="mt-8 rounded-3xl bg-slate-50 p-5"><p className="font-bold text-slate-900">Completion Certificate</p><div id="printable-certificate" className="mx-auto mt-3 max-w-md rounded-2xl border bg-white p-4 text-sm leading-relaxed shadow-sm"><p className="text-center text-lg font-black">APA Formatting Lab Completion Certificate</p><p className="mt-3 text-center">This certifies that</p><p className="text-center text-xl font-black text-emerald-700">{studentName}</p><p className="mt-2 text-center">successfully completed the APA Formatting Lab.</p><p className="mt-3 text-center text-xs text-slate-500">Instructor: Professor Wendy Esch</p><p className="mt-2 text-center text-xs text-slate-500">Certificate ID: {certificateId}</p></div><div className="mt-4 rounded-2xl bg-yellow-50 p-4 text-left ring-1 ring-yellow-200"><p className="font-black text-yellow-900">IMPORTANT: Submit your certificate in Blackboard</p><ol className="mt-2 list-decimal space-y-1 pl-5 text-sm text-yellow-950"><li>View your APA Formatting Lab Completion Certificate.</li><li>Save the certificate as a PDF.</li><li>Upload the certificate only to the APA Formatting Lab assignment link in Blackboard for participation credit.</li><li>Open the APA Essay Template and click the blue <strong>Use Template</strong> button.</li></ol></div><Button onClick={downloadCertificate} className="mt-4 rounded-2xl bg-emerald-700 hover:bg-emerald-800">View Completion Certificate</Button>{certificateDownloaded && <div className="mt-3 rounded-2xl bg-green-50 p-4 text-left text-sm text-green-800"><p className="font-bold">✓ Certificate ready.</p><p>Use the button below to print this page or save it as a PDF, then upload the certificate to Blackboard.</p><Button onClick={printCertificate} className="mt-3 rounded-2xl bg-green-700 hover:bg-green-800">Save Certificate as PDF</Button><p className="mt-2 text-xs text-green-700">In the print window, choose Save as PDF, then upload that PDF to Blackboard.</p></div>}<a href={templateUrl} target="_blank" rel="noreferrer" onClick={openTemplate} className="mt-3 inline-flex w-full items-center justify-center rounded-2xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-800 hover:bg-slate-50">Open APA Essay Template</a>{templateOpened && <p className="mt-2 text-sm font-semibold text-green-700">✓ Template opened. Click the blue Use Template button in Google Docs.</p>}<a href={templateUrl} target="_blank" rel="noreferrer" className="mt-2 block text-sm font-semibold text-blue-700 underline">Template link not opening? Click here.</a><p className="mt-3 text-left text-sm text-slate-600">After the template opens, click the blue <strong>Use Template</strong> button. Replace the Paper Title, Your Full Name, Full College/University Name, Course Name and Number, Instructor Name, and Assignment Due Date placeholders. Save the file somewhere in Google Drive where you can find it all semester.</p><p className="mt-2 text-left text-sm font-semibold text-slate-700">Important: This is an APA template and should only be used for assignments that require APA formatting.</p></div></motion.div></div></div>;
}

function PageSetup({ onCancel, onApply }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><h2 className="text-2xl font-black text-slate-950">Page setup</h2><p className="mt-2 text-sm text-slate-600">Check the margins for the document. APA uses 1-inch margins on all sides.</p><div className="mt-5 grid grid-cols-2 gap-3">{["Top", "Bottom", "Left", "Right"].map(label => <label key={label} className="rounded-2xl border bg-slate-50 p-3 text-sm font-semibold text-slate-700">{label}<select className="mt-2 w-full rounded-xl border bg-white px-3 py-2" defaultValue="1 inch"><option>0.5 inch</option><option>1 inch</option><option>1.5 inches</option></select></label>)}</div><div className="mt-6 flex gap-3"><Button variant="outline" className="flex-1 rounded-2xl" onClick={onCancel}>Cancel</Button><Button className="flex-1 rounded-2xl bg-emerald-700 hover:bg-emerald-800" onClick={onApply}>Apply</Button></div></div></div>; }

function TopBar({ steps, state, stepIndex, setStepIndex, progress, current, helpLevel, openMenu, setOpenMenu, setShowPageSetup, completeStepAction, showWrongAction }) { return <div className="border-b bg-white shadow-sm"><div className="bg-gradient-to-r from-emerald-600 via-sky-500 to-amber-400 px-5 py-3 text-white"><div className="mb-2 flex items-center justify-between gap-3"><div className="flex items-center gap-2 font-bold"><Sparkles className="h-5 w-5" /> APA Lab Progress</div><div className="text-sm font-semibold">Step {stepIndex + 1} of {steps.length}</div></div><div className="flex w-full gap-1 rounded-full bg-white/25 p-1">{steps.map((step, i) => { const done = step.check(state); const active = i === stepIndex; return <button key={step.id} onClick={() => { if (i <= stepIndex || step.check(state)) setStepIndex(i); }} title={step.short} className={`h-4 flex-1 rounded-full transition-all ${done ? `bg-gradient-to-r ${step.color} shadow-sm` : active ? "bg-white" : "bg-white/30"}`} />; })}</div></div><div className="flex items-center gap-3 px-5 py-3"><div className="flex h-10 w-10 items-center justify-center rounded-lg bg-emerald-100 text-emerald-700"><FileText className="h-6 w-6" /></div><div><h1 className="text-lg font-semibold leading-tight">APA Practice Worksheet</h1><p className="text-xs text-slate-500">Google Docs-style practice workspace</p></div><div className="ml-auto rounded-full bg-green-50 px-4 py-2 text-sm font-semibold text-green-700">{progress}% Complete</div></div><div className="flex flex-wrap items-center gap-1 px-5 pb-2 text-sm">{[{ label: "File", key: "file" }, { label: "Edit", key: "edit" }, { label: "View", key: "view" }, { label: "Insert", key: "insert" }, { label: "Format", key: "format" }, { label: "Tools", key: "tools" }, { label: "Help", key: "help" }].map(menu => <div className="relative" key={menu.key}><button onClick={() => setOpenMenu(openMenu === menu.key ? null : menu.key)} className={`rounded px-3 py-1 hover:bg-slate-100 ${current.id === "pageNumber" && menu.key === "insert" && helpLevel >= 2 ? "ring-4 ring-yellow-300" : ""}`}>{menu.label}</button>{openMenu === menu.key && <div className="absolute left-0 top-8 z-30 w-64 rounded-lg border bg-white p-2 shadow-xl">{menu.key === "file" && <button onClick={() => { if (current.id !== "margins") { showWrongAction(); setOpenMenu(null); return; } setShowPageSetup(true); setOpenMenu(null); }} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Page setup</button>}{menu.key === "insert" && <button onClick={() => completeStepAction("pageNumber", { pageNumberAdded: true, runningHeadRemoved: true })} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Page number → Top right</button>}{menu.key === "format" && <><button onClick={() => completeStepAction("font", { font: "Times New Roman" })} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Font → Times New Roman</button><button onClick={() => completeStepAction("size", { fontSize: "12" })} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Font size → 12</button><button onClick={() => showWrongAction("Use the Line Spacing toolbar button for this step.")} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Line spacing</button><button onClick={() => completeStepAction("titlePageCentering", { titlePageCentered: true })} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">Align → Center</button></>}{!["file", "insert", "format"].includes(menu.key) && <p className="px-3 py-2 text-slate-500">Use File, Insert, or Format for this lab.</p>}</div>}</div>)}</div><div className="flex flex-wrap items-center gap-2 border-t px-5 py-2 text-sm"><button className="rounded p-2 hover:bg-slate-100"><RotateCcw className="h-4 w-4" /></button><div className="h-6 w-px bg-slate-300" /><ToolbarMenu label={state.font} items={["Arial", "Times New Roman", "Calibri"]} openKey="font" openMenu={openMenu} setOpenMenu={setOpenMenu} onChoose={font => completeStepAction("font", { font })} highlight={current.id === "font" && helpLevel >= 2} wide /><ToolbarMenu label={state.fontSize} items={["10", "11", "12", "14"]} openKey="size" openMenu={openMenu} setOpenMenu={setOpenMenu} onChoose={fontSize => completeStepAction("size", { fontSize })} highlight={current.id === "size" && helpLevel >= 2} /><button className="rounded p-2 hover:bg-slate-100"><Type className="h-4 w-4" /></button><button onClick={() => completeStepAction("boldTitle", { titleBold: true })} className={`rounded p-2 hover:bg-slate-100 ${current.id === "boldTitle" && helpLevel >= 2 ? "ring-4 ring-yellow-300" : ""}`}><Bold className="h-4 w-4" /></button><button onClick={() => completeStepAction("titlePageCentering", { titlePageCentered: true })} className="rounded p-2 hover:bg-slate-100"><AlignLeft className="h-4 w-4" /></button><button onClick={() => completeStepAction("titlePageCentering", { titlePageCentered: true })} className={`rounded p-2 hover:bg-slate-100 ${current.id === "titlePageCentering" && helpLevel >= 2 ? "ring-4 ring-yellow-300" : ""}`}><AlignCenter className="h-4 w-4" /></button><div className="relative"><button onClick={() => setOpenMenu(openMenu === "lineSpacing" ? null : "lineSpacing")} className={`rounded p-2 hover:bg-slate-100 ${current.id === "spacing" && helpLevel >= 2 ? "ring-4 ring-yellow-300" : ""}`} title="Line Spacing"><LineSpacingIcon /></button>{openMenu === "lineSpacing" && <div className="absolute z-30 mt-1 w-36 rounded-lg border bg-white p-1 shadow-xl">{["Single", "1.15", "1.5", "Double"].map(spacing => <button key={spacing} onClick={() => completeStepAction("spacing", { spacing })} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">{spacing}</button>)}</div>}</div></div></div>; }
function ToolbarMenu({ label, items, openKey, openMenu, setOpenMenu, onChoose, highlight, wide }) { return <div className="relative"><button onClick={() => setOpenMenu(openMenu === openKey ? null : openKey)} className={`${wide ? "min-w-44" : "w-20"} flex items-center justify-between rounded border bg-white px-3 py-1 hover:bg-slate-50 ${highlight ? "ring-4 ring-yellow-300" : ""}`}>{label} <ChevronDown className="h-4 w-4" /></button>{openMenu === openKey && <div className={`${wide ? "w-44" : "w-20"} absolute z-30 mt-1 rounded-lg border bg-white p-1 shadow-xl`}>{items.map(item => <button key={item} onClick={() => onChoose(item)} className="w-full rounded px-3 py-2 text-left hover:bg-blue-50">{item}</button>)}</div>}</div>; }

function Paper({ state, current, showPageTwo, pageStyle, activeClass, setSelectedArea }) { return <div className="relative min-h-[900px] w-full max-w-[720px] overflow-hidden bg-white shadow-2xl" style={{ padding: state.margins === "1 inch" ? "92px" : "52px" }}>{state.pageNumberAdded && <div className="absolute right-20 top-12 text-right" style={pageStyle}>{showPageTwo ? "2" : "1"}</div>}<div style={pageStyle}>{showPageTwo ? <div><div className="mb-4 rounded-2xl bg-emerald-50 p-3 text-center text-sm font-bold text-emerald-800">APA Student Paper — Page 2 of 2</div><div className="text-center font-bold">{state.title === correctTitle ? correctTitle : "The Effects of Social Media on Student Success"}</div><div className={`mt-6 text-center ${state.levelOneHeadingFormatted ? "font-bold" : ""} ${current.id === "heading" ? "ring-4 ring-sky-300 bg-sky-50/60 rounded-md p-1" : ""}`}>Optional Level 1 Heading</div><p onClick={() => setSelectedArea("indent")} className={`mt-4 cursor-pointer break-words p-1 ${current.id === "indent" ? "" : activeClass("indent")}`} style={{ textIndent: state.paragraphIndented ? "0.5in" : "0in", overflowWrap: "break-word" }}>{paragraphText}</p></div> : <div><div className="mb-4 text-xs text-slate-400">{!state.runningHeadRemoved && "RUNNING HEAD: SOCIAL MEDIA AND STUDENT SUCCESS"}</div><div className={state.titlePlaced ? "pt-16" : "pt-0"}><div onClick={() => setSelectedArea("title")} className={`cursor-pointer break-words p-1 ${activeClass("titleCase")} ${activeClass("boldTitle")} ${activeClass("titlePageCentering")}`} style={{ textAlign: state.titlePageCentered ? "center" : "left", fontWeight: state.titleBold ? "700" : "400" }}>{state.title}</div><div className={`${state.blankLineAfterTitle ? "mt-8" : "mt-2"} ${state.titlePageCentered ? "text-center" : "text-left"} ${current.id === "studentInfo" ? "ring-4 ring-sky-300 bg-sky-50/60 rounded-md p-2" : ""}`}><div>Sarah Mitchell</div><div>Full College/University Name</div><div>Course Name and Number</div><div>Instructor Name</div><div className={current.id === "dateFormat" ? "ring-4 ring-sky-300 bg-sky-50/60 rounded-md" : ""}>{state.selectedDate}</div></div></div></div>}</div></div>; }

function Coach({ current, state, feedback, wrongAction, currentComplete, stepIndex, steps, helpLevel, setHelpLevel, setFeedback, setStepIndex, completeStepAction, choices, chooseFromList, chooseTitle, chooseDate, chooseOrder, pressTab, isLastStepComplete, nextStep, setShowCertificate, showPageTwo }) { return <Card className={`rounded-3xl border-0 bg-gradient-to-br ${current.color} p-[2px] shadow-xl`}><CardContent className="rounded-3xl bg-white p-5"><div className="mb-3 flex items-center gap-2 text-slate-700"><Highlighter className="h-5 w-5" /><span className="font-bold">APA Coach</span></div><p className="text-sm font-bold uppercase tracking-wide text-slate-500">Step {stepIndex + 1} of {steps.length}</p><h2 className="mt-1 text-2xl font-black text-slate-950">{current.goal}</h2><div className="mt-4 rounded-2xl bg-blue-50 p-4"><p className="text-sm font-bold uppercase tracking-wide text-blue-700">What to do</p><ol className="mt-2 list-decimal space-y-2 pl-5 text-sm text-slate-800">{current.doThis.map(item => <li key={item}>{item}</li>)}</ol></div>{showPageTwo && <div className="mt-4 rounded-2xl bg-emerald-50 p-4 text-sm text-emerald-900"><strong>You are on Page 2.</strong> The title page is complete. Page 2 is where the actual writing begins, the page number continues, and the title appears again at the top.</div>}{current.custom === "placement" && <ChoiceBox title="Look at the page above. Where should the APA title begin?" choices={choices.placement} selected={state.titlePlaced ? choices.placement[1] : ""} onSelect={c => chooseFromList(c, choices.placement[1], { titlePlaced: true }, "Correct! The title should begin about 3–4 lines down from the top.", "Not quite. The APA title should begin about 3–4 lines down from the top.")} />}{current.custom === "centering" && <ChoiceBox title="Look at the page above. How should the title page alignment be fixed?" choices={choices.centering} selected={state.titlePageCentered ? choices.centering[0] : ""} onSelect={c => chooseFromList(c, choices.centering[0], { titlePageCentered: true }, "Correct! Center the title page information, but keep the page number on the right.", "Not quite. Center the title page information except for the page number.")} />}{current.custom === "pageNumber" && <div className="mt-4 rounded-2xl bg-white p-4 ring-1 ring-slate-200"><p className="text-sm font-bold uppercase tracking-wide text-slate-500">APA student paper header</p><p className="mt-2 text-sm text-slate-700">Student papers usually need only the page number in the top-right header. Click Insert → Page number → Top right.</p></div>}{current.custom === "bold" && <ChoiceBox title="Which element should be bolded on the APA title page?" choices={choices.bold} selected={state.titleBold ? choices.bold[0] : ""} onSelect={c => chooseFromList(c, choices.bold[0], { titleBold: true }, "Correct! Bold the paper title only.", "Not quite. Only the paper title should be bolded.")} />}{current.custom === "blank" && <ChoiceBox title="What spacing belongs between the title and student name?" choices={choices.blank} selected={state.blankLineAfterTitle ? choices.blank[0] : ""} onSelect={c => chooseFromList(c, choices.blank[0], { blankLineAfterTitle: true }, "Correct! APA title pages use one extra double-spaced blank line after the title.", "Not quite. Add one extra double-spaced blank line between the title and student name.")} />}{current.custom === "title" && <ChoiceBox title="Look at the page above. How can the title be fixed?" subtitle="Select one of the multiple-choice answers below." choices={choices.title} selected={state.title} onSelect={chooseTitle} />}{current.custom === "order" && <ChoiceBox title="Which order is correct for an APA student title page?" choices={choices.order} selected={state.titlePageOrderCorrect ? choices.order[0] : ""} onSelect={chooseOrder} />}{current.custom === "date" && <ChoiceBox title="Look at the page above. How should the due date be fixed?" subtitle="Select the correct date below." choices={choices.date} selected={state.selectedDate} onSelect={chooseDate} />}{current.custom === "tab" && <div className="mt-4 rounded-2xl bg-white p-4 ring-1 ring-slate-200"><p className="text-sm font-bold uppercase tracking-wide text-slate-500">Practice the Tab key on Page 2</p><p className="mt-2 text-sm text-slate-700">The blue Press Tab button is in this APA Coach panel. Click it and watch the first line on Page 2 move inward.</p><Button onClick={pressTab} className="mt-3 w-full rounded-2xl bg-emerald-700 hover:bg-emerald-800">Press Tab</Button></div>}{current.custom === "heading" && <ChoiceBox title="If a writer uses a Level 1 APA heading, how should it be formatted?" subtitle="Headings are optional unless your instructor requires them." choices={choices.heading} selected={state.levelOneHeadingFormatted ? choices.heading[0] : ""} onSelect={c => chooseFromList(c, choices.heading[0], { levelOneHeadingFormatted: true }, "Correct! A Level 1 APA heading is centered and bold.", "Not quite. A Level 1 APA heading should be centered and bold.")} />}<div className="mt-4 rounded-2xl bg-yellow-50 p-4"><p className="text-sm font-bold uppercase tracking-wide text-yellow-700">Why</p><p className="mt-1 text-sm text-slate-800">{current.why}</p></div><div className="mt-4 flex items-center gap-2 rounded-2xl bg-slate-50 p-3">{currentComplete ? <CheckCircle2 className="h-6 w-6 text-green-600" /> : <MousePointerClick className="h-6 w-6 text-slate-500" />}<span className={`font-semibold ${currentComplete ? "text-green-700" : "text-slate-700"}`}>{currentComplete ? current.success : "Not complete yet. Try the steps above."}</span></div>{feedback && <div className={`mt-3 rounded-2xl p-4 text-sm font-semibold ${feedback.startsWith("Correct") ? "bg-green-50 text-green-800" : "bg-red-50 text-red-800"}`}>{feedback}</div>}{wrongAction && <div className="mt-3 rounded-2xl bg-red-100 p-4 text-sm font-bold text-red-800">{wrongAction}</div>}{!currentComplete && <div className="mt-4 space-y-3"><Button variant="outline" className="w-full rounded-2xl" onClick={() => setHelpLevel(l => Math.min(l + 1, 3))}><HelpCircle className="mr-2 h-4 w-4" /> I’m Stuck</Button>{helpLevel >= 1 && <div className="rounded-2xl bg-purple-50 p-4 text-sm text-purple-950"><strong>Hint:</strong> {current.hint}</div>}{helpLevel >= 2 && <div className="rounded-2xl bg-amber-50 p-4 text-sm text-amber-950"><strong>Show me:</strong> {current.showMe}</div>}{helpLevel >= 3 && <Button className="w-full rounded-2xl bg-amber-600 hover:bg-amber-700" onClick={() => completeStepAction(current.id, current.solve)}>Complete This Step For Me</Button>}</div>}<div className="mt-5 flex gap-3"><Button variant="outline" className="flex-1 rounded-2xl" onClick={() => { setHelpLevel(0); setFeedback(""); setStepIndex(i => Math.max(0, i - 1)); }} disabled={stepIndex === 0}>Back</Button><Button className="flex-1 rounded-2xl bg-emerald-700 hover:bg-emerald-800" onClick={isLastStepComplete ? () => setShowCertificate(true) : nextStep} disabled={!currentComplete}>{isLastStepComplete ? "Finish Lab" : "Next"}</Button></div></CardContent></Card>; }

function ProgressCard({ steps, state, stepIndex, setStepIndex, completedCount, progress, showExample, setShowExample }) { return <Card className="rounded-3xl shadow-sm"><CardContent className="p-5"><div className="mb-3 flex items-center justify-between"><p className="font-bold">Progress</p><p className="text-sm font-semibold text-slate-500">{completedCount}/{steps.length}</p></div><Progress value={progress} className="mb-4" /><div className="grid grid-cols-2 gap-2 text-sm">{steps.map((step, i) => <button key={step.id} onClick={() => { if (i <= stepIndex || step.check(state)) setStepIndex(i); }} className={`flex items-center gap-2 rounded-xl p-2 text-left hover:bg-slate-100 ${i === stepIndex ? "bg-emerald-50" : ""}`}>{step.check(state) ? <CheckCircle2 className="h-4 w-4 text-green-600" /> : <span className="h-4 w-4 rounded-full border" />} {step.short}</button>)}</div><Button variant="outline" onClick={() => setShowExample(!showExample)} className="mt-4 w-full rounded-2xl">{showExample ? "Hide" : "View"} Correct APA Example</Button>{showExample && <div className="mt-4 rounded-2xl border bg-white p-4 font-serif text-xs leading-loose shadow-inner"><div className="text-right">1</div><div className="mt-8 text-center font-bold">The Effects of Social Media on Student Success</div><div className="h-4" /><div className="text-center">Sarah Mitchell</div><div className="text-center">Full College/University Name</div><div className="text-center">Course Name and Number</div><div className="text-center">Instructor Name</div><div className="text-center">June 15, 2026</div><div className="mt-8 text-right">2</div><div className="text-center font-bold">The Effects of Social Media on Student Success</div><div className="mt-3 text-center font-bold">Optional Level 1 Heading</div><p style={{ textIndent: "0.5in" }}>Students use digital tools every day to communicate, learn, and complete assignments.</p></div>}</CardContent></Card>; }
